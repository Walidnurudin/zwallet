import React from "react";

function ConfirmationTransfer() {
  return (
    <div
      style={{
        padding: "30px",
        boxShadow: "0px 4px 20px rgba(0, 0, 0, 0.05)",
        borderRadius: "25px",
        marginTop: "40px",
        background: "#ffffff",
      }}
    >
      <div>Confirmation Transfer</div>
    </div>
  );
}

export default ConfirmationTransfer;
